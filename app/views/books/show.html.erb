<h2>Show a book</h2>

<div class="book_container">
	<%= image_tag @book.cover.url %>
	<h3><%= @book.book_name %></h3>
	<p>Description: <%= @book.description %></p>
	<p>Author: <%= @book.author %></p>
	<p><%= @book.group_id %></p>
	<%= link_to "Edit", edit_book_path(group_id: @book.group_id) %>
	<%= link_to "Delete book", book_path(@book, group_id: @group.id), method: :delete, data: { confirm: "Are you sure?"} %>

<div class="form-container">
<%= form_for(@comment.new) do |f| %>
	<p>Add a comment</p>
	<%= f.text_area :content %>
	<%= f.hidden_field :profile_id, :value => current_user.profile.id %>
	<%= f.hidden_field :book_id, :value => @book.id %>
	<%= f.submit %>
<% end %>
</div>

<% @comments.each do |comment| %>
	<div class="comment_container">
		<%= image_tag comment.profile.avatar.url(:thumb)  %>
		<p><%= comment.content %></p>
		<small>Posted by: <%= comment.profile.first_name %> <%= comment.profile.first_name %></small>
		<%= link_to "Delete comment", comment_path(comment), method: :delete, data: { confirm: "Are you sure?"} %>
	</div>
<% end %>

</div>